# 🚗 Collision Detection Package

**Пакет для обнаружения столкновений и вычисления площадей пересечения прямоугольников**

## 📦 О пакете

`collision` — это Python-пакет для работы с прямоугольниками, предоставляющий функционал для проверки корректности, обнаружения столкновений и вычисления площадей пересечения.

## ✨ Возможности

- ✅ **Проверка корректности** прямоугольников
- 🔄 **Обнаружение столкновений** между прямоугольниками
- 📐 **Вычисление площади** пересечения двух прямоугольников
- 🧮 **Множественное пересечение** — уникальная площадь пересечения нескольких прямоугольников
- 🛡️ **Обработка ошибок** с кастомными исключениями

## 🚀 Быстрый старт

### Установка

```bash
# Клонируйте репозиторий
git clone <your-repository-url>
cd your-repository

# Импортируйте пакет
from collision import *
```

### Примеры использования

```python
from collision import isCorrectRect, isCollisionRect, intersectionAreaRect, intersectionAreaMultiRect

# Проверка корректности прямоугольника
print(isCorrectRect([(-3.4, 1), (9.2, 10)]))  # True
print(isCorrectRect([(-7, 9), (3, 6)]))       # False

# Обнаружение столкновений
rect1 = [(-3.4, 1), (9.2, 10)]
rect2 = [(-7.4, 0), (13.2, 12)]
print(isCollisionRect(rect1, rect2))  # True

# Площадь пересечения двух прямоугольников
area = intersectionAreaRect(rect1, rect2)
print(f"Площадь пересечения: {area}")

# Множественное пересечение
rectangles = [
    [(-3, 1), (9, 10)],
    [(-7, 0), (13, 12)],
    [(0, 0), (5, 5)]
]
result = intersectionAreaMultiRect(rectangles)
print(f"Уникальная площадь: {result}")
```

## 📚 Документация API

### 🔧 `isCorrectRect(points)`
Проверяет корректность координат прямоугольника.

**Параметры:**
- `points` — список из двух кортежей `[(x1,y1), (x2,y2)]`

**Возвращает:** `bool`

---

### 🎯 `isCollisionRect(rect1, rect2)`
Проверяет пересекаются ли два прямоугольника.

**Параметры:**
- `rect1`, `rect2` — прямоугольники в формате `[(x1,y1), (x2,y2)]`

**Возвращает:** `bool`  
**Исключения:** `RectCorrectError` при некорректных данных

---

### 📐 `intersectionAreaRect(rect1, rect2)`
Вычисляет площадь пересечения двух прямоугольников.

**Параметры:**
- `rect1`, `rect2` — прямоугольники в формате `[(x1,y1), (x2,y2)]`

**Возвращает:** `float` (0 если нет пересечения)  
**Исключения:** `ValueError` при некорректных данных

---

### 🧮 `intersectionAreaMultiRect(rectangles)`
Вычисляет уникальную площадь пересечения нескольких прямоугольников.

**Параметры:**
- `rectangles` — список прямоугольников

**Возвращает:** `float`  
**Исключения:** `RectCorrectError` при некорректных данных

## 🏗️ Структура проекта

```
collision/
├── __init__.py              # Инициализация пакета
├── rect_check.py            # Проверка корректности + исключения
├── collision_detection.py   # Обнаружение столкновений
├── intersection_area.py     # Площадь пересечения двух прямоугольников
└── multi_intersection.py    # Множественное пересечение
```

## 🧪 Тестирование

```python
# Примеры тестов
from collision import *

# Тест корректности
assert isCorrectRect([(0, 0), (5, 5)]) == True
assert isCorrectRect([(5, 0), (0, 5)]) == False

# Тест столкновений
rect_a = [(0, 0), (4, 4)]
rect_b = [(2, 2), (6, 6)]
assert isCollisionRect(rect_a, rect_b) == True

# Тест площади пересечения
assert intersectionAreaRect(rect_a, rect_b) == 4.0
```

## ⚠️ Обработка ошибок

Пакет использует кастомное исключение `RectCorrectError`:

```python
try:
    isCollisionRect([(1, 1), (2, 2)], [(3, 17), (13, 1)])
except RectCorrectError as e:
    print(f"Ошибка: {e}")
```

## 🔍 Алгоритмы

- **Принцип включения-исключения** — для множественных пересечений
- **Аксиоматическая геометрия** — для проверки столкновений
- **Эффективные вычисления** — минимальное количество операций

## 📄 Лицензия

MIT License — подробнее в файле [LICENSE](LICENSE).

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для фичи (`git checkout -b feature/amazing-feature`)
3. Закоммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

---

**⭐ Если проект полезен — поставьте звезду!**